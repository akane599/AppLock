package dev.pranav.applock.features.lockscreen.ui

/** Retains the old internal component name while using the full, secure authentication screen. */
class TransparentBiometricActivity : PasswordOverlayActivity()
